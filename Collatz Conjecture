#include<bits/stdc++.h>
using namespace std;

using ll = long long;

signed main(){
    ios::sync_with_stdio(false);
    cin.tie(0);

    ll t;
    cin >> t;

    while( t-- ){
        ll k,n;
        cin >> k >> n;

        for(int i = 0; i < k ; i++){
            n = n*2;
        }
        cout << n << endl;
    }

}

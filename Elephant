#include<bits/stdc++.h>
using namespace std;
 
#define rep(i,n) for(int i = 0; i < n; i++)
using ll = long long;
 
signed main(){
    ll n;
    cin>>n;
    int step = 5;
    ll op = 0;
    while(n != 0){
        op+= n/step;
        n = (n%step);
        step--;
    }
    cout<<op<<endl;
}

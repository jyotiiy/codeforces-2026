#include<bits/stdc++.h>
using namespace std;

using ll = long long;

int main(){
    string s;
    cin>>s;
    unordered_map<char,int> um;
    for(char c: s){
        um[c]++;
    }
    vector<pair<char,int>> v(um.begin(),um.end());
    sort(v.begin(),v.end());

    auto len = v[v.size()-1];
    for(int i = 0; i < len.second; i++){
        cout<<len.first;
    }
}

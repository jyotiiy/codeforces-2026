#include<bits/stdc++.h>
using namespace std;
 
using ll = long long;
#define rep(i,n) for(int i = 0; i <n; i++)
 
void solve(){
    ll n,q;
 
    cin>>n>>q;
    vector<ll> a(n);
    vector<ll> b(n);
 
    rep(i,n) cin>>a[i];
    rep(i,n) cin>>b[i];
 
    a[n-1] = max(a[n-1],b[n-1]);
 
    for(int i = n-2; i >= 0; i--){
        a[i] = max({a[i],a[i+1],b[i]});
    }
    
    for(int i = 1; i < n; i++){
        a[i] += a[i-1];
    }
    rep(k,q){
        ll l,r;
        cin>>l>>r;
        
        l--; r--;
        ll left = (l - 1) < 0? 0: a[l-1];
        ll right =  a[r];
 
        cout<< right-left<<" ";
    }
    cout<<endl;
 
}
signed main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
 
    ll t;
    cin>>t;
    while(t--){
        solve();
    }
}

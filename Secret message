#include <bits/stdc++.h>
using namespace std;
 
 
#define JAI_SHREE_RAM ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
 
 
#define ull unsigned long long
#define ll long long
#define ld long double
 
#define py cout << "YES\n";
#define pn cout << "NO\n";
 
 
#define all(x) (x).begin(), (x).end()
#define rep(i, n) for (ll i = 0; i < (n); i++)
 
 
const ll mod  = 1e9 + 7;
const ll mod1 = 998244353;
const ll N    = 5e4 + 10;
 
 
vector<vector<int>> facs(N);
 
 
void compute_factors() {
    for (int i = 1; i < N; i++) {
        for (int j = i; j < N; j += i) {
            facs[j].push_back(i);
        }
    }
}
 
signed main(){
    JAI_SHREE_RAM;
    ll t;
    cin >> t;
    while( t-- ){
        ll n,k;
        cin >> n >> k;
        
        vector<vector<ll>> a(26,vector<ll>(n,0));
 
        for(ll i = 0; i < k; i++){
            string str;
            cin >> str;
            for(ll j = 0; j < n; j++){
                a[str[j]-'a'][j] = 1;
            }
        }
        string ans = "";
        vector<ll> fact;
        for(ll i = 1; i <= n; i++){
            if(n % i == 0){
                fact.push_back(i);
            }
        }
 
        for(auto x : fact){
            string temp = "";
            ll i = 0;
            for(i = 0; i < x; i++){
                ll j = 0;
                for(j = 0; j < 26; j++){
                    ll p = i;
                    for(p = i; p < n; p +=x){
                        if(a[j][p] == 0) break;
                    }
                    if(p >= n){
                        temp.push_back((char)(j+'a'));
                        break;
                    }
                }
            }
            if(temp.size() == x){
                ans = temp;
                break;
            }
        }
     
        string newAns = "";
        ll d = ans.size();
        for(ll i = 0; i < n; i++){
            newAns.push_back(ans[i%d]);
        }
        cout << newAns << endl;
 
    }
 
    
 
}

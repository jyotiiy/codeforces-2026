#include<bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i = 0; i < n; i++)

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n;
    cin>>n;
    if(n == 2 || (n % 2 != 0)){
        cout<<"NO"<<endl;
    }else{
        cout<<"YES"<<endl;
    }
}
